1. **CONFIRMED** — The baseline sweep found no lost step, refusal, check, or threshold. Removed summaries route to surviving detail: loading thresholds remain at `.agents/skills/enterprise-bootstrap/references/bootstrap-reference.md:526`, density rules at `.agents/skills/enterprise-bootstrap/references/bootstrap-reference.md:472`, and journey typing semantics at `.agents/skills/orkestrel-human-journey/SKILL.md:66`.

2. **BROKEN** — The full HEAD files still contain aphorisms and human-explainer clauses. “Structure is information” is an aphorism, while the hired-designer comparison explains and persuades a person rather than directing an agent. `.agents/skills/enterprise-bootstrap/references/frontend-design.md:19`, `.agents/skills/enterprise-bootstrap/references/frontend-design.md:33`. This violates `AGENTS.md:155` and `AGENTS.md:166`.

3. **BROKEN** — Changed lines restate laws owned elsewhere instead of pointing to them. The negative-control requirement at `.agents/skills/enterprise-bootstrap/SKILL.md:78` duplicates `.claude/rules/quality.md:61`. The rendered-evidence rule at `.agents/skills/enterprise-bootstrap/SKILL.md:72` duplicates `.agents/orchestration.md:750`. Both are changed text in `tmp/audit/campaign.patch:91` and `tmp/audit/campaign.patch:97`.

4. **CONFIRMED** — Direct byte comparison found both bridge descriptions identical to their canonical descriptions. The matching descriptions are at `.agents/skills/orkestrel-human-journey/SKILL.md:3`, `.claude/skills/orkestrel-human-journey/SKILL.md:3`, `.agents/skills/orkestrel-polish-surface/SKILL.md:3`, and `.claude/skills/orkestrel-polish-surface/SKILL.md:3`. After substituting the skill name, the human-journey bridge body matches the falsify bridge at `.claude/skills/orkestrel-human-journey/SKILL.md:6` and `.claude/skills/orkestrel-falsify/SKILL.md:6`.

5. **CONFIRMED** — The file has one root `interface` mapping, three non-empty fields, and the literal `$orkestrel-human-journey` token in `default_prompt`. `.agents/skills/orkestrel-human-journey/agents/openai.yaml:1`, `.agents/skills/orkestrel-human-journey/agents/openai.yaml:2`, `.agents/skills/orkestrel-human-journey/agents/openai.yaml:3`, `.agents/skills/orkestrel-human-journey/agents/openai.yaml:4`.

6. **BROKEN** — Discovery, exact-case file checks, one-level references, exclusion control, and the recorded red proof exist at `tests/setupPolicy.ts:773`, `tests/setupPolicy.ts:839`, `tests/setupPolicy.ts:877`, `tests/setupPolicy.ts:1227`, and `tmp/audit/w3-red-proof-output.txt:12`. However, metadata validation does not enforce exactly claim 5’s invariant. It additionally requires exactly four lines, fixed key order, fixed indentation, and single-quoted scalars. Valid double-quoted YAML therefore fails at `tests/setupPolicy.ts:803` and `tests/setupPolicy.ts:807`.

7. **BROKEN** — A direct parser probe using a conforming double-quoted YAML mapping returned `false`, although the same probe accepted a 5,000-character description and zero references. The false rejection follows from the single-quote-only regular expressions at `tests/setupPolicy.ts:807`. The 55-green real-tree runs at `tmp/audit/w3-red-proof-output.txt:6` and `tmp/audit/w3-red-proof-output.txt:20` do not prove soundness for other conforming serializations.

8. **CONFIRMED** — The polish description sync preserves the canonical trigger scope in `tmp/audit/campaign.patch:1046`. The four aphorism recasts preserve the original steering on distinctiveness, deliberate risk, screenshot review, restraint, and persistent notes while converting those passages into actionable directions. `tmp/audit/campaign.patch:570`, `tmp/audit/campaign.patch:579`, `.agents/skills/enterprise-bootstrap/references/frontend-design.md:7`, `.agents/skills/enterprise-bootstrap/references/frontend-design.md:45`. Claim 2 fails on other text already present in the full file, not these recasts.

9. **CONFIRMED** — The whole range contains exactly the stated fourteen paths and no others. `tmp/audit/full-stat.txt:1`, `tmp/audit/full-stat.txt:14`, `tmp/audit/full-stat.txt:15`. The merge precedes the five campaign commits, supporting the stated provenance of `REDESIGN.md`. `tmp/audit/commits.txt:1`, `tmp/audit/commits.txt:6`. The supplied status record is empty at `tmp/audit/status.txt:1`.

VERDICT: 5 CONFIRMED, 4 BROKEN, 0 UNRESOLVED, 0 NOT-EVIDENCED